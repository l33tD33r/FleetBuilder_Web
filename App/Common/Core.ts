enum AttackDie {
    Red,
    Blue,
    Black
}

enum Faction {
    Imperial,
    Rebel
}

enum Stat {
    Speed,
    HullPoints,
    SquadronAttack,
    ShipAttack
}

enum DefenseToken {
    Brace,
    Contain,
    Evade,
    Redirect,
    Scatter
}